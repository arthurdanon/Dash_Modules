# ====== Core API ======
NODE_ENV=development
PORT=4000
APP_URL="https://modulo.apps.arthurdanon.fr"   # <— AJOUT : URL publique du FRONT pour les liens d’invitation

# ====== Base de données ======
DATABASE_URL="postgresql://taskflow:taskflow@localhost:5432/taskflow?schema=public"

# ====== JWT / Auth ======
# Secrets (garde-les hors Git ; en prod: variables Dokploy)
ACCESS_SECRET="sY9J2lDuzgNQ4w7V1mQFzA9UuBvKsC3e"
REFRESH_SECRET="y4Rt9Fp2LgBqV1nJ0sZpK8qD6wNcX5hG"

# Durées (match lib/auth.js)
JWT_ACCESS_TTL=12h
JWT_REFRESH_TTL=7d

# Métadonnées (match lib/auth.js)
JWT_ISSUER=taskflow-core
JWT_AUDIENCE=taskflow-clients

# Chiffrement des MDP en clair (AES-GCM) — 32 bytes en base64
PASSWORD_ENC_KEY="RzjXc2fmp+E8tA+fKZ7O7n5bNqtR/vUINvOnz2y0tXo="

# ====== Bootstrapping ======
COMPANY_NAME="Default Company"
USERNAME_ADMIN="Arthur"
USERNAME_PASSWORD="Arthur.225"
ADMIN_EMAIL="admin@admin.fr"

# ====== Swagger / CORS / Proxy ======
SWAGGER=ON
CORS=ON
CORS_ORIGINS=http://localhost:3000
TRUST_PROXY=OFF   # (PROD derrière proxy/Nginx: ON)

# ====== Rate-limit (optionnel si tu wires ces vars dans server.js) ======
RATE_LIMIT_AUTH_WINDOW_MS=600000  # 10 min
RATE_LIMIT_AUTH_MAX=100           # 100 req / fenêtre


# ====== Mailer (optionnel) ======
SMTP_HOST=smtp.mail.ovh.net
SMTP_PORT=465
SMTP_USER=contact@arthurdanon.fr
SMTP_PASS=Arthur.225
SMTP_FROM="${COMPANY_NAME} <contact@arthurdanon.fr>"