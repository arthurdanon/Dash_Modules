// prisma/schema.prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

/**
 * ===================== Core tables =====================
 */

/**
 * Rôles fixes en table (Admin, Owner, Manager, User)
 */
model CoreRole {
  id        String     @id @default(cuid())
  name      String     @unique // 'ADMIN' | 'OWNER' | 'MANAGER' | 'USER'
  isAdmin   Boolean    @default(false)
  isOwner   Boolean    @default(false)
  isManager Boolean    @default(false)
  users     CoreUser[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("coreroles")
}

/**
 * Paramétrage global (quotas + catalogue de modules activables)
 */
model CoreSetting {
  id               String @id @default(cuid())
  name             String @unique // COMPANY_NAME
  // Quotas globaux (null = illimité)
  maxSites         Int?
  maxOwners        Int?
  maxManagers      Int?
  maxUsers         Int?
  // Catalogue des modules activables (ex: { stock: true })
  availableModules Json?

  sites CoreSite[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("coresettings")
}

/**
 * Site (appartient au CoreSetting) + modules activés localement
 */
model CoreSite {
  id   String @id @default(cuid())
  name String @unique

  setting   CoreSetting @relation(fields: [settingId], references: [id])
  settingId String

  // Propriétaire (owner) optionnel du site
  ownerId String?
  owner   CoreUser? @relation("SiteOwner", fields: [ownerId], references: [id])

  // Modules activés pour CE site (ex: { stock: true })
  modules Json?

  // Back-relations
  members      CoreSiteMember[]
  teams        CoreTeam[]
  primaryUsers CoreUser[]       @relation("PrimarySite") // back-rel du site principal des users

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("coresites")
}

/**
 * Utilisateur core (rôle global + 0..N sites via membership + 0..1 team)
 */
model CoreUser {
  id       String @id @default(cuid())
  username String @unique

  email String? @unique
  firstName String
  lastName  String

  roleId String
  role   CoreRole @relation(fields: [roleId], references: [id])

  primarySiteId String?
  primarySite   CoreSite? @relation("PrimarySite", fields: [primarySiteId], references: [id])

  ownedSites CoreSite[] @relation("SiteOwner")

  teamId String?
  team   CoreTeam? @relation(fields: [teamId], references: [id])

  managedTeams CoreTeam[] @relation("TeamManager")

  // 👇 change en nullable pour permettre l’invitation sans mot de passe
  passwordHash String?

  mustChangePwd Boolean @default(true)
  isActive      Boolean @default(true)
  tokenVersion  Int     @default(0)

  memberships CoreSiteMember[]

  // 👇 back-rel vers les tokens d’auth
  authTokens CoreAuthToken[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("coreusers")
}

// 👇 Types de token pour invitation / reset
enum CoreAuthTokenType {
  INVITE
  RESET
}

// 👇 Table de tokens (single-use, expirables)
model CoreAuthToken {
  id        String            @id @default(cuid())
  type      CoreAuthTokenType
  userId    String
  user      CoreUser          @relation(fields: [userId], references: [id])
  tokenHash String            @unique
  expiresAt DateTime
  usedAt    DateTime?
  meta      Json?

  createdAt DateTime @default(now())

  @@index([userId, type])
  @@map("coreauthtokens")
}

/**
 * Membership user↔site (accès) + flag manager au niveau du site
 */
model CoreSiteMember {
  id String @id @default(cuid())

  siteId String
  site   CoreSite @relation(fields: [siteId], references: [id])

  userId String
  user   CoreUser @relation(fields: [userId], references: [id])

  // Si ce user est “manager du site”
  isManager Boolean @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([siteId, userId])
  @@map("coresitemembers")
}

/**
 * Équipe appartenant à un site
 * - managerId optionnel (le “manager officiel” de l’équipe)
 * - membres = users avec user.teamId = cette équipe
 */
model CoreTeam {
  id     String   @id @default(cuid())
  siteId String
  site   CoreSite @relation(fields: [siteId], references: [id])

  name String

  managerId String?
  manager   CoreUser? @relation("TeamManager", fields: [managerId], references: [id])

  members CoreUser[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([siteId, name])
  @@map("coreteams")
}
